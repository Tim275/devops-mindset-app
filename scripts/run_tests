#!/bin/bash
echo "🧪 Running Tests..."
eval "$(mise activate bash)"
cd /workspaces/devops-projekt  # ← Navigate to project root

# Add project root to PYTHONPATH
export PYTHONPATH="/workspaces/devops-projekt:$PYTHONPATH"

# Run tests from project root
python -m pytest src/frontend/tests/ -v
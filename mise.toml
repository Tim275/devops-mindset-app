[tools]
python = "3.12"                    # Consistent Python version
uv = "latest"                      # Modern package management
ruff = "latest"                    # Code quality
pre-commit = "latest"              # Automated quality gates
gh = "latest"                      # GitHub CLI
trivy = "latest"                  # Security scanning
###  ! Note: commitizen is not available via mise, hence it is not installed, so write a script for it


[env]
APP_REPO = 'devops-projekt'
GH_REPO = 'devops-projekt'


[settings]
experimental = true
idiomatic_version_file_enable_tools = ["python"]